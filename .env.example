# Copie para .env — usado pelo docker-compose (docker compose lê .env na raiz automaticamente).
ADMIN_EMAIL=admin@suaempresa.com
ADMIN_PASSWORD=troque-esta-senha

# Opcionais — ver docs/DEPLOY.md
# APP_URL=http://localhost:8080
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# EMAIL_FROM=Bolão Copa AMM <no-reply@ammcompany.com>
